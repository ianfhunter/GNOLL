name: "Test: Javascript"

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: javascript prerequisites
      run: sudo apt install nodejs emscripten
    - name: make js
      run: make ks
